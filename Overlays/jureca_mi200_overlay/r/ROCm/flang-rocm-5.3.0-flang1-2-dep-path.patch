diff --git a/runtime/flang/CMakeLists.txt b/runtime/flang/CMakeLists.txt
index ae6c5e01..f015cfe3 100644
--- a/runtime/flang/CMakeLists.txt
+++ b/runtime/flang/CMakeLists.txt
@@ -465,8 +465,17 @@ if(EXISTS bin/flang1)
   set(FLANG2_DEP bin/flang2)
 else()
   # when just building runtime, flang1 flang2 binaries should have been installed
-  set(FLANG1_DEP ${CMAKE_INSTALL_PREFIX}${DEVEL_PACKAGE}/bin/flang1)
-  set(FLANG2_DEP ${CMAKE_INSTALL_PREFIX}${DEVEL_PACKAGE}/bin/flang2)
+  set(FLANG_BIN_PATH "${CMAKE_INSTALL_PREFIX}${DEVEL_PACKAGE}/bin")
+  message(STATUS "Looking if flang{1,2} exist in ${FLANG_BIN_PATH}")
+  set(FLANG1_DEP "${FLANG_BIN_PATH}/flang1")
+  set(FLANG2_DEP "${FLANG_BIN_PATH}/flang2")
+  if(NOT EXISTS "${FLANG1_DEP}")
+      message(FATAL_ERROR "${FLANG1_DEP} does not exist")
+  endif()
+  if(NOT EXISTS "${FLANG2_DEP}")
+      message(FATAL_ERROR "${FLANG2_DEP} does not exist")
+  endif()
+  list(APPEND CMAKE_PROGRAM_PATH "${FLANG_BIN_PATH}")
 endif()
 
 # Fortran files with macros as module names need to be preprocessed.
diff --git a/runtime/flangrti/CMakeLists.txt b/runtime/flangrti/CMakeLists.txt
index 3e1d7f09..a020a65c 100644
--- a/runtime/flangrti/CMakeLists.txt
+++ b/runtime/flangrti/CMakeLists.txt
@@ -97,6 +97,7 @@ if (NOT DEFINED LIBOMP_EXPORT_DIR)
     HINTS
     ${CMAKE_BINARY_DIR}/lib
     ${OPENMP_BUILD_DIR}
+    PATH_SUFFIXES runtime/lib
     NO_DEFAULT_PATH)
     message (STATUS "FLANGRTI ${FLANG_LIBOMP}")
   target_link_libraries(flangrti_shared ${FLANG_LIBOMP})
