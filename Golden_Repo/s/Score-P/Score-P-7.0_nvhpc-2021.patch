--- a/build-score/configure	2021-08-17 15:37:03.902432489 +0200
+++ b/build-score/configure	2021-08-18 12:00:24.884805172 +0200
@@ -2356,8 +2356,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -3192,8 +3192,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C++ compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C++ compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -3242,8 +3242,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -3308,8 +3308,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C++ compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -3367,8 +3367,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -4890,8 +4890,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -9735,8 +9735,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -10728,9 +10728,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -10775,7 +10774,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -11424,16 +11423,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -14580,8 +14579,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -15550,7 +15549,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -15650,7 +15649,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -16403,8 +16402,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -16436,8 +16435,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
--- a/build-mpi/configure	2021-08-17 15:37:03.906432554 +0200
+++ b/build-mpi/configure	2021-08-18 12:00:24.888805234 +0200
@@ -4385,8 +4385,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -6010,8 +6010,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -6125,8 +6125,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -6175,8 +6175,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -6241,8 +6241,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -6300,8 +6300,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -7531,8 +7531,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7540,8 +7540,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -7549,8 +7549,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7597,8 +7597,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -7606,8 +7606,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -7615,8 +7615,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -7624,8 +7624,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -7637,8 +7637,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7646,8 +7646,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -7655,8 +7655,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7671,8 +7671,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7680,8 +7680,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -7689,8 +7689,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7705,8 +7705,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7714,8 +7714,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7723,8 +7723,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7736,8 +7736,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7745,8 +7745,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7764,8 +7764,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -7773,8 +7773,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -7797,8 +7797,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7806,8 +7806,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7815,8 +7815,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7835,8 +7835,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -7851,8 +7851,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7866,8 +7866,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7881,8 +7881,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -7898,8 +7898,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7913,8 +7913,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7928,8 +7928,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -7950,8 +7950,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -7969,8 +7969,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -7984,8 +7984,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -7999,8 +7999,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8020,8 +8020,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -8035,8 +8035,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -8050,8 +8050,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8088,8 +8088,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -8104,8 +8104,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -8119,8 +8119,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -8156,8 +8156,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -8217,8 +8217,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -8226,8 +8226,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -8239,8 +8239,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -8248,8 +8248,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -8257,8 +8257,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8277,8 +8277,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -8292,8 +8292,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -8307,8 +8307,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8320,8 +8320,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -8329,8 +8329,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -8351,8 +8351,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8364,8 +8364,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8376,8 +8376,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8390,8 +8390,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -8399,8 +8399,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -8408,8 +8408,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8420,8 +8420,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -8429,8 +8429,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -8442,8 +8442,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -8451,8 +8451,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -8460,8 +8460,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8483,8 +8483,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -8498,8 +8498,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -8516,8 +8516,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -9097,8 +9097,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9106,8 +9106,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9115,8 +9115,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9163,8 +9163,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC__%100" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9172,8 +9172,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9181,8 +9181,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_cxx_compiler_version_build"        ""; then :
@@ -9190,8 +9190,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch.$_ax_cxx_compiler_version_build"
@@ -9203,8 +9203,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__ibmxl_release__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9212,8 +9212,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__ibmxl_version__" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9221,8 +9221,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9237,8 +9237,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9246,8 +9246,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9255,8 +9255,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9271,8 +9271,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9280,8 +9280,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9289,8 +9289,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9302,8 +9302,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__clang_minor__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9311,8 +9311,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__clang_patchlevel__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9330,8 +9330,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9339,8 +9339,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9363,8 +9363,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9372,8 +9372,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9381,8 +9381,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9401,8 +9401,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_cxx_compiler_version_until59" = X1; then :
@@ -9417,8 +9417,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9432,8 +9432,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9447,8 +9447,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9464,8 +9464,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9479,8 +9479,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9494,8 +9494,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9516,8 +9516,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_cxx_compiler_version_untilA0121" = X1; then :
@@ -9535,8 +9535,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9550,8 +9550,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9565,8 +9565,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9586,8 +9586,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9601,8 +9601,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9616,8 +9616,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9654,8 +9654,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_cxx_compiler_version_turboc_raw -lt 661 || test $_ax_cxx_compiler_version_turboc_raw -gt 1023; then :
@@ -9670,8 +9670,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9685,8 +9685,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_cxx_compiler_version="0turboc:$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9722,8 +9722,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_cxx_compiler_version_borlandc_raw in #(
@@ -9783,8 +9783,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9792,8 +9792,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9805,8 +9805,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9814,8 +9814,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9823,8 +9823,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9843,8 +9843,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9858,8 +9858,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9873,8 +9873,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9886,8 +9886,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9895,8 +9895,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_cxx_compiler_version_patch=0
@@ -9917,8 +9917,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9930,8 +9930,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9942,8 +9942,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9956,8 +9956,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9965,8 +9965,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9974,8 +9974,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9986,8 +9986,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9995,8 +9995,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -10008,8 +10008,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -10017,8 +10017,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -10026,8 +10026,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -10049,8 +10049,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -10064,8 +10064,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "
@@ -10082,8 +10082,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -10969,8 +10969,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "linking to Fortran libraries from C fails" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "linking to Fortran libraries from C fails${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -11131,8 +11131,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compile a simple Fortran program" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compile a simple Fortran program${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext

@@ -12276,8 +12276,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "linking to Fortran libraries from C fails" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "linking to Fortran libraries from C fails${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -12454,8 +12454,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compile a simple Fortran program" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compile a simple Fortran program${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext

@@ -16574,8 +16574,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -17582,9 +17582,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -17629,7 +17628,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -18278,16 +18277,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -21562,8 +21561,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -22564,7 +22563,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -22664,7 +22663,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -23417,8 +23416,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -23450,8 +23449,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
@@ -25342,9 +25341,8 @@
 	lt_prog_compiler_pic_F77='-fPIC -DPIC'
 	lt_prog_compiler_static_F77='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl_F77='-Wl,'
 	lt_prog_compiler_pic_F77='-fpic'
 	lt_prog_compiler_static_F77='-Bstatic'
@@ -25389,7 +25387,7 @@
 	  lt_prog_compiler_pic_F77='-hpic'
 	  lt_prog_compiler_static_F77='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl_F77='-Wl,'
 	  lt_prog_compiler_pic_F77='-fpic'
 	  lt_prog_compiler_static_F77='-Bstatic'
@@ -26023,16 +26021,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec_F77='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec_F77='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec_F77='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -28611,9 +28609,8 @@
 	lt_prog_compiler_pic_FC='-fPIC -DPIC'
 	lt_prog_compiler_static_FC='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl_FC='-Wl,'
 	lt_prog_compiler_pic_FC='-fpic'
 	lt_prog_compiler_static_FC='-Bstatic'
@@ -28658,7 +28655,7 @@
 	  lt_prog_compiler_pic_FC='-hpic'
 	  lt_prog_compiler_static_FC='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl_FC='-Wl,'
 	  lt_prog_compiler_pic_FC='-fpic'
 	  lt_prog_compiler_static_FC='-Bstatic'
@@ -29292,16 +29289,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec_FC='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec_FC='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec_FC='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -31407,8 +31404,8 @@
   *) :
     { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "unable to determine executable link mode" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; } ;;
+as_fn_error $? "unable to determine executable link mode${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; } ;;
 esac
                  $as_echo "executable link mode: $afs_link_type (auto-detected)" >&5
 fi
@@ -31423,8 +31420,8 @@
   if test "x$enable_shared" = xyes && test "x$enable_static" = xno; then :
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "static linking with only shared libraries not possible" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "static linking with only shared libraries not possible${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
              afs_linkmode_LDFLAGS="-all-static"
 fi
@@ -31434,8 +31431,8 @@
          if test "x$enable_shared" = xyes && test "x$enable_static" = xno; then :
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "static linking with only shared libraries not possible" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "static linking with only shared libraries not possible${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
          afs_linkmode_LDFLAGS="-all-static" ;; #(
   *) :
@@ -32823,8 +32820,8 @@
 if test x = x"$MPILIBS"; then
         { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "MPI C compiler does not work. If you intended to build Score-P without support for the MPI programming model, please reconfigure using --without-mpi." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "MPI C compiler does not work. If you intended to build Score-P without support for the MPI programming model, please reconfigure using --without-mpi.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
         :
 else
         scorep_mpi_c_supported="yes"
@@ -32849,8 +32846,8 @@
   if test "$ac_cv_type_void_p" = yes; then
      { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "cannot compute sizeof (void *)" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "cannot compute sizeof (void *)${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
    else
      ac_cv_sizeof_void_p=0
    fi
@@ -33689,8 +33686,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -33698,8 +33695,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -33707,8 +33704,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33787,8 +33784,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -33796,8 +33793,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -33805,8 +33802,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -33814,8 +33811,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -33827,8 +33824,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33836,8 +33833,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -33845,8 +33842,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33861,8 +33858,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -33870,8 +33867,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -33879,8 +33876,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33895,8 +33892,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33904,8 +33901,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -33913,8 +33910,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33926,8 +33923,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33935,8 +33932,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -33954,8 +33951,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -33963,8 +33960,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -33987,8 +33984,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33996,8 +33993,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -34005,8 +34002,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34025,8 +34022,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -34041,8 +34038,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -34056,8 +34053,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -34071,8 +34068,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34088,8 +34085,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -34103,8 +34100,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -34118,8 +34115,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34140,8 +34137,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -34159,8 +34156,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -34174,8 +34171,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -34189,8 +34186,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34210,8 +34207,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -34225,8 +34222,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -34240,8 +34237,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34294,8 +34291,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -34310,8 +34307,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -34325,8 +34322,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -34362,8 +34359,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -34423,8 +34420,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -34432,8 +34429,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -34445,8 +34442,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -34454,8 +34451,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -34463,8 +34460,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34483,8 +34480,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -34498,8 +34495,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -34513,8 +34510,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34526,8 +34523,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -34535,8 +34532,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -34557,8 +34554,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34570,8 +34567,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34582,8 +34579,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34596,8 +34593,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -34605,8 +34602,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -34614,8 +34611,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34626,8 +34623,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -34635,8 +34632,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -34648,8 +34645,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -34657,8 +34654,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -34666,8 +34663,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34689,8 +34686,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -34704,8 +34701,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -34722,8 +34719,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -56373,8 +56370,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no suitable timer detected." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no suitable timer detected.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 SCOREP_TIMER_CPPFLAGS=${scorep_timer_cppflags}
--- a/build-gcc-plugin/configure	2021-08-17 15:37:03.910432619 +0200
+++ b/build-gcc-plugin/configure	2021-08-18 12:00:24.896805354 +0200
@@ -2349,8 +2349,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -3970,8 +3970,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -4085,8 +4085,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -4135,8 +4135,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -4201,8 +4201,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -4260,8 +4260,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -10229,8 +10229,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -11221,9 +11221,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -11268,7 +11267,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -11917,16 +11916,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -15073,8 +15072,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -16043,7 +16042,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -16143,7 +16142,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -16896,8 +16895,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -16929,8 +16928,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
--- a/build-libwrap/configure	2021-08-17 15:37:03.910432619 +0200
+++ b/build-libwrap/configure	2021-08-18 12:00:24.900805416 +0200
@@ -2553,8 +2553,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -4363,8 +4363,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -4478,8 +4478,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -4528,8 +4528,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -4594,8 +4594,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -4653,8 +4653,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -10262,8 +10262,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -11254,9 +11254,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -11301,7 +11300,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -11950,16 +11949,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -15106,8 +15105,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -16076,7 +16075,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -16176,7 +16175,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -16929,8 +16928,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -16962,8 +16961,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
--- a/build-backend/configure	2021-08-17 15:37:03.914432683 +0200
+++ b/build-backend/configure	2021-08-18 12:00:24.900805416 +0200
@@ -4193,8 +4193,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -5707,8 +5707,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -5822,8 +5822,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -5872,8 +5872,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -5938,8 +5938,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -5997,8 +5997,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -7228,8 +7228,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7237,8 +7237,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -7246,8 +7246,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7294,8 +7294,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -7303,8 +7303,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -7312,8 +7312,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -7321,8 +7321,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -7334,8 +7334,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7343,8 +7343,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -7352,8 +7352,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7368,8 +7368,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7377,8 +7377,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -7386,8 +7386,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7402,8 +7402,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7411,8 +7411,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7420,8 +7420,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7433,8 +7433,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7442,8 +7442,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7461,8 +7461,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -7470,8 +7470,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -7494,8 +7494,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7503,8 +7503,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7512,8 +7512,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7532,8 +7532,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -7548,8 +7548,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7563,8 +7563,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7578,8 +7578,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -7595,8 +7595,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7610,8 +7610,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7625,8 +7625,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -7647,8 +7647,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -7666,8 +7666,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -7681,8 +7681,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -7696,8 +7696,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7717,8 +7717,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -7732,8 +7732,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -7747,8 +7747,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7785,8 +7785,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -7801,8 +7801,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -7816,8 +7816,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -7853,8 +7853,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -7914,8 +7914,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -7923,8 +7923,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -7936,8 +7936,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7945,8 +7945,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -7954,8 +7954,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7974,8 +7974,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -7989,8 +7989,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -8004,8 +8004,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8017,8 +8017,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -8026,8 +8026,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -8048,8 +8048,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8061,8 +8061,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8073,8 +8073,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8087,8 +8087,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -8096,8 +8096,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -8105,8 +8105,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8117,8 +8117,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -8126,8 +8126,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -8139,8 +8139,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -8148,8 +8148,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -8157,8 +8157,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8180,8 +8180,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -8195,8 +8195,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -8213,8 +8213,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8794,8 +8794,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -8803,8 +8803,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -8812,8 +8812,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -8860,8 +8860,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC__%100" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -8869,8 +8869,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -8878,8 +8878,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_cxx_compiler_version_build"        ""; then :
@@ -8887,8 +8887,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch.$_ax_cxx_compiler_version_build"
@@ -8900,8 +8900,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__ibmxl_release__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -8909,8 +8909,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__ibmxl_version__" "_ax_cxx_compiler_version_major"        ""; then :
@@ -8918,8 +8918,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -8934,8 +8934,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -8943,8 +8943,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -8952,8 +8952,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -8968,8 +8968,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -8977,8 +8977,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -8986,8 +8986,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -8999,8 +8999,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__clang_minor__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9008,8 +9008,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__clang_patchlevel__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9027,8 +9027,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9036,8 +9036,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9060,8 +9060,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9069,8 +9069,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9078,8 +9078,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9098,8 +9098,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_cxx_compiler_version_until59" = X1; then :
@@ -9114,8 +9114,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9129,8 +9129,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9144,8 +9144,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9161,8 +9161,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9176,8 +9176,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9191,8 +9191,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9213,8 +9213,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_cxx_compiler_version_untilA0121" = X1; then :
@@ -9232,8 +9232,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9247,8 +9247,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9262,8 +9262,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9283,8 +9283,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9298,8 +9298,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9313,8 +9313,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9351,8 +9351,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_cxx_compiler_version_turboc_raw -lt 661 || test $_ax_cxx_compiler_version_turboc_raw -gt 1023; then :
@@ -9367,8 +9367,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9382,8 +9382,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_cxx_compiler_version="0turboc:$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9419,8 +9419,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_cxx_compiler_version_borlandc_raw in #(
@@ -9480,8 +9480,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9489,8 +9489,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9502,8 +9502,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9511,8 +9511,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9520,8 +9520,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9540,8 +9540,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9555,8 +9555,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9570,8 +9570,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9583,8 +9583,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9592,8 +9592,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_cxx_compiler_version_patch=0
@@ -9614,8 +9614,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9627,8 +9627,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9639,8 +9639,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9653,8 +9653,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9662,8 +9662,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9671,8 +9671,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9683,8 +9683,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9692,8 +9692,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9705,8 +9705,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9714,8 +9714,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9723,8 +9723,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9746,8 +9746,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -9761,8 +9761,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "
@@ -9779,8 +9779,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -10666,8 +10666,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "linking to Fortran libraries from C fails" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "linking to Fortran libraries from C fails${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -10828,8 +10828,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compile a simple Fortran program" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compile a simple Fortran program${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext

@@ -11973,8 +11973,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "linking to Fortran libraries from C fails" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "linking to Fortran libraries from C fails${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -12151,8 +12151,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compile a simple Fortran program" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compile a simple Fortran program${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext

@@ -16232,8 +16232,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -17240,9 +17240,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -17287,7 +17286,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -17936,16 +17935,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -21220,8 +21219,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -22222,7 +22221,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -22322,7 +22321,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -23075,8 +23074,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -23108,8 +23107,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
@@ -25000,9 +24999,8 @@
 	lt_prog_compiler_pic_F77='-fPIC -DPIC'
 	lt_prog_compiler_static_F77='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl_F77='-Wl,'
 	lt_prog_compiler_pic_F77='-fpic'
 	lt_prog_compiler_static_F77='-Bstatic'
@@ -25047,7 +25045,7 @@
 	  lt_prog_compiler_pic_F77='-hpic'
 	  lt_prog_compiler_static_F77='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl_F77='-Wl,'
 	  lt_prog_compiler_pic_F77='-fpic'
 	  lt_prog_compiler_static_F77='-Bstatic'
@@ -25681,16 +25679,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec_F77='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec_F77='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec_F77='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -28269,9 +28267,8 @@
 	lt_prog_compiler_pic_FC='-fPIC -DPIC'
 	lt_prog_compiler_static_FC='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl_FC='-Wl,'
 	lt_prog_compiler_pic_FC='-fpic'
 	lt_prog_compiler_static_FC='-Bstatic'
@@ -28316,7 +28313,7 @@
 	  lt_prog_compiler_pic_FC='-hpic'
 	  lt_prog_compiler_static_FC='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl_FC='-Wl,'
 	  lt_prog_compiler_pic_FC='-fpic'
 	  lt_prog_compiler_static_FC='-Bstatic'
@@ -28950,16 +28947,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec_FC='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec_FC='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec_FC='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -31065,8 +31062,8 @@
   *) :
     { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "unable to determine executable link mode" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; } ;;
+as_fn_error $? "unable to determine executable link mode${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; } ;;
 esac
                  $as_echo "executable link mode: $afs_link_type (auto-detected)" >&5
 fi
@@ -31081,8 +31078,8 @@
   if test "x$enable_shared" = xyes && test "x$enable_static" = xno; then :
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "static linking with only shared libraries not possible" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "static linking with only shared libraries not possible${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
              afs_linkmode_LDFLAGS="-all-static"
 fi
@@ -31092,8 +31089,8 @@
          if test "x$enable_shared" = xyes && test "x$enable_static" = xno; then :
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "static linking with only shared libraries not possible" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "static linking with only shared libraries not possible${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
          afs_linkmode_LDFLAGS="-all-static" ;; #(
   *) :
@@ -37377,8 +37374,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -37386,8 +37383,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -37395,8 +37392,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37475,8 +37472,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -37484,8 +37481,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -37493,8 +37490,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -37502,8 +37499,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -37515,8 +37512,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -37524,8 +37521,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -37533,8 +37530,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37549,8 +37546,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -37558,8 +37555,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -37567,8 +37564,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37583,8 +37580,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -37592,8 +37589,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -37601,8 +37598,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37614,8 +37611,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -37623,8 +37620,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -37642,8 +37639,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -37651,8 +37648,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -37675,8 +37672,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -37684,8 +37681,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -37693,8 +37690,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37713,8 +37710,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -37729,8 +37726,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -37744,8 +37741,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -37759,8 +37756,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -37776,8 +37773,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -37791,8 +37788,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -37806,8 +37803,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -37828,8 +37825,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -37847,8 +37844,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -37862,8 +37859,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -37877,8 +37874,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37898,8 +37895,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -37913,8 +37910,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -37928,8 +37925,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -37982,8 +37979,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -37998,8 +37995,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -38013,8 +38010,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -38050,8 +38047,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -38111,8 +38108,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -38120,8 +38117,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -38133,8 +38130,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -38142,8 +38139,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -38151,8 +38148,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -38171,8 +38168,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -38186,8 +38183,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -38201,8 +38198,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -38214,8 +38211,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -38223,8 +38220,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -38245,8 +38242,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -38258,8 +38255,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -38270,8 +38267,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -38284,8 +38281,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -38293,8 +38290,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -38302,8 +38299,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -38314,8 +38311,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -38323,8 +38320,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -38336,8 +38333,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -38345,8 +38342,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -38354,8 +38351,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -38377,8 +38374,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -38392,8 +38389,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -38410,8 +38407,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -40124,8 +40121,8 @@
   if test "$ac_cv_type_void_p" = yes; then
      { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "cannot compute sizeof (void *)" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "cannot compute sizeof (void *)${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
    else
      ac_cv_sizeof_void_p=0
    fi
@@ -40168,8 +40165,8 @@
   if test "$ac_cv_type_double" = yes; then
      { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "cannot compute sizeof (double)" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "cannot compute sizeof (double)${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
    else
      ac_cv_sizeof_double=0
    fi
@@ -49253,8 +49250,8 @@
   if test "$ac_cv_type_int64_t" = yes; then
      { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "cannot compute sizeof (int64_t)" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "cannot compute sizeof (int64_t)${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
    else
      ac_cv_sizeof_int64_t=0
    fi
@@ -51515,8 +51512,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no suitable timer detected." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no suitable timer detected.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 SCOREP_TIMER_CPPFLAGS=${scorep_timer_cppflags}
--- a/build-shmem/configure	2021-08-17 15:37:03.918432750 +0200
+++ b/build-shmem/configure	2021-08-18 12:00:24.912805596 +0200
@@ -4216,8 +4216,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -5838,8 +5838,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -5953,8 +5953,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -6003,8 +6003,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -6069,8 +6069,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -6128,8 +6128,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -7359,8 +7359,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7368,8 +7368,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -7377,8 +7377,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7425,8 +7425,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -7434,8 +7434,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -7443,8 +7443,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -7452,8 +7452,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -7465,8 +7465,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7474,8 +7474,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -7483,8 +7483,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7499,8 +7499,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -7508,8 +7508,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -7517,8 +7517,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7533,8 +7533,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7542,8 +7542,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7551,8 +7551,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7564,8 +7564,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7573,8 +7573,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7592,8 +7592,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -7601,8 +7601,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -7625,8 +7625,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -7634,8 +7634,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -7643,8 +7643,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7663,8 +7663,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -7679,8 +7679,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7694,8 +7694,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7709,8 +7709,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -7726,8 +7726,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7741,8 +7741,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -7756,8 +7756,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -7778,8 +7778,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -7797,8 +7797,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -7812,8 +7812,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -7827,8 +7827,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7848,8 +7848,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -7863,8 +7863,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -7878,8 +7878,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -7916,8 +7916,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -7932,8 +7932,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -7947,8 +7947,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -7984,8 +7984,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -8045,8 +8045,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -8054,8 +8054,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -8067,8 +8067,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -8076,8 +8076,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -8085,8 +8085,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8105,8 +8105,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -8120,8 +8120,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -8135,8 +8135,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8148,8 +8148,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -8157,8 +8157,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -8179,8 +8179,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8192,8 +8192,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8204,8 +8204,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -8218,8 +8218,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -8227,8 +8227,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -8236,8 +8236,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8248,8 +8248,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -8257,8 +8257,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -8270,8 +8270,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -8279,8 +8279,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -8288,8 +8288,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8311,8 +8311,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -8326,8 +8326,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -8344,8 +8344,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -8925,8 +8925,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -8934,8 +8934,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -8943,8 +8943,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -8991,8 +8991,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC__%100" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9000,8 +9000,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9009,8 +9009,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_cxx_compiler_version_build"        ""; then :
@@ -9018,8 +9018,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch.$_ax_cxx_compiler_version_build"
@@ -9031,8 +9031,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__ibmxl_release__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9040,8 +9040,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_cxx_compute_int "$LINENO" "__ibmxl_version__" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9049,8 +9049,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9065,8 +9065,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9074,8 +9074,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9083,8 +9083,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9099,8 +9099,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9108,8 +9108,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9117,8 +9117,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9130,8 +9130,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__clang_minor__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9139,8 +9139,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__clang_patchlevel__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9158,8 +9158,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9167,8 +9167,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9191,8 +9191,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9200,8 +9200,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9209,8 +9209,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9229,8 +9229,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_cxx_compiler_version_until59" = X1; then :
@@ -9245,8 +9245,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9260,8 +9260,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9275,8 +9275,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9292,8 +9292,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9307,8 +9307,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9322,8 +9322,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9344,8 +9344,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_cxx_compiler_version_untilA0121" = X1; then :
@@ -9363,8 +9363,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9378,8 +9378,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9393,8 +9393,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9414,8 +9414,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9429,8 +9429,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "((
@@ -9444,8 +9444,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9482,8 +9482,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_cxx_compiler_version_turboc_raw -lt 661 || test $_ax_cxx_compiler_version_turboc_raw -gt 1023; then :
@@ -9498,8 +9498,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9513,8 +9513,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_cxx_compiler_version="0turboc:$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9550,8 +9550,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_cxx_compiler_version_borlandc_raw in #(
@@ -9611,8 +9611,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9620,8 +9620,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9633,8 +9633,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9642,8 +9642,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9651,8 +9651,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9671,8 +9671,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9686,8 +9686,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(
@@ -9701,8 +9701,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9714,8 +9714,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9723,8 +9723,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_cxx_compiler_version_patch=0
@@ -9745,8 +9745,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9758,8 +9758,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9770,8 +9770,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -9784,8 +9784,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9793,8 +9793,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9802,8 +9802,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9814,8 +9814,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_cxx_compiler_version_major"        ""; then :
@@ -9823,8 +9823,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor"
@@ -9836,8 +9836,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_cxx_compiler_version_minor"        ""; then :
@@ -9845,8 +9845,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_cxx_compiler_version_patch"        ""; then :
@@ -9854,8 +9854,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -9877,8 +9877,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -9892,8 +9892,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_cxx_compute_int "$LINENO" "
@@ -9910,8 +9910,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_cxx_compiler_version="$_ax_cxx_compiler_version_major.$_ax_cxx_compiler_version_minor.$_ax_cxx_compiler_version_patch"
@@ -10797,8 +10797,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "linking to Fortran libraries from C fails" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "linking to Fortran libraries from C fails${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -10959,8 +10959,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compile a simple Fortran program" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compile a simple Fortran program${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext

@@ -12104,8 +12104,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "linking to Fortran libraries from C fails" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "linking to Fortran libraries from C fails${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -12282,8 +12282,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compile a simple Fortran program" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compile a simple Fortran program${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f core conftest.err conftest.$ac_objext conftest.$ac_ext

@@ -16355,8 +16355,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -17363,9 +17363,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -17410,7 +17409,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -18059,16 +18058,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -21343,8 +21342,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -22345,7 +22344,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -22445,7 +22444,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -23198,8 +23197,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -23231,8 +23230,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
@@ -25123,9 +25122,8 @@
 	lt_prog_compiler_pic_F77='-fPIC -DPIC'
 	lt_prog_compiler_static_F77='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl_F77='-Wl,'
 	lt_prog_compiler_pic_F77='-fpic'
 	lt_prog_compiler_static_F77='-Bstatic'
@@ -25170,7 +25168,7 @@
 	  lt_prog_compiler_pic_F77='-hpic'
 	  lt_prog_compiler_static_F77='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl_F77='-Wl,'
 	  lt_prog_compiler_pic_F77='-fpic'
 	  lt_prog_compiler_static_F77='-Bstatic'
@@ -25804,16 +25802,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec_F77='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec_F77='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec_F77='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -28392,9 +28390,8 @@
 	lt_prog_compiler_pic_FC='-fPIC -DPIC'
 	lt_prog_compiler_static_FC='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl_FC='-Wl,'
 	lt_prog_compiler_pic_FC='-fpic'
 	lt_prog_compiler_static_FC='-Bstatic'
@@ -28439,7 +28436,7 @@
 	  lt_prog_compiler_pic_FC='-hpic'
 	  lt_prog_compiler_static_FC='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl_FC='-Wl,'
 	  lt_prog_compiler_pic_FC='-fpic'
 	  lt_prog_compiler_static_FC='-Bstatic'
@@ -29073,16 +29070,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec_FC='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec_FC='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec_FC='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -31188,8 +31185,8 @@
   *) :
     { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "unable to determine executable link mode" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; } ;;
+as_fn_error $? "unable to determine executable link mode${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; } ;;
 esac
                  $as_echo "executable link mode: $afs_link_type (auto-detected)" >&5
 fi
@@ -31204,8 +31201,8 @@
   if test "x$enable_shared" = xyes && test "x$enable_static" = xno; then :
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "static linking with only shared libraries not possible" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "static linking with only shared libraries not possible${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
              afs_linkmode_LDFLAGS="-all-static"
 fi
@@ -31215,8 +31212,8 @@
          if test "x$enable_shared" = xyes && test "x$enable_static" = xno; then :
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "static linking with only shared libraries not possible" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "static linking with only shared libraries not possible${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
          afs_linkmode_LDFLAGS="-all-static" ;; #(
   *) :
@@ -32585,8 +32582,8 @@
   if test "$ac_cv_type_void_p" = yes; then
      { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "cannot compute sizeof (void *)" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "cannot compute sizeof (void *)${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
    else
      ac_cv_sizeof_void_p=0
    fi
@@ -33289,8 +33286,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -33298,8 +33295,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -33307,8 +33304,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33387,8 +33384,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -33396,8 +33393,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -33405,8 +33402,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -33414,8 +33411,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -33427,8 +33424,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33436,8 +33433,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -33445,8 +33442,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33461,8 +33458,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -33470,8 +33467,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -33479,8 +33476,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33495,8 +33492,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33504,8 +33501,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -33513,8 +33510,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33526,8 +33523,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33535,8 +33532,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -33554,8 +33551,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -33563,8 +33560,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -33587,8 +33584,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -33596,8 +33593,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -33605,8 +33602,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33625,8 +33622,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -33641,8 +33638,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -33656,8 +33653,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -33671,8 +33668,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -33688,8 +33685,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -33703,8 +33700,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -33718,8 +33715,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -33740,8 +33737,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -33759,8 +33756,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -33774,8 +33771,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -33789,8 +33786,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33810,8 +33807,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -33825,8 +33822,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -33840,8 +33837,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -33894,8 +33891,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -33910,8 +33907,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -33925,8 +33922,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -33962,8 +33959,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -34023,8 +34020,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -34032,8 +34029,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -34045,8 +34042,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -34054,8 +34051,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -34063,8 +34060,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34083,8 +34080,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -34098,8 +34095,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -34113,8 +34110,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34126,8 +34123,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -34135,8 +34132,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -34157,8 +34154,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34170,8 +34167,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34182,8 +34179,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -34196,8 +34193,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -34205,8 +34202,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -34214,8 +34211,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34226,8 +34223,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -34235,8 +34232,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -34248,8 +34245,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -34257,8 +34254,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -34266,8 +34263,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -34289,8 +34286,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -34304,8 +34301,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -34322,8 +34319,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
--- a/configure	2021-08-17 15:37:03.922432814 +0200
+++ b/configure	2021-08-18 12:00:24.916805658 +0200
@@ -2010,8 +2010,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

--- a/build-frontend/configure	2021-08-17 15:37:03.922432814 +0200
+++ b/build-frontend/configure	2021-08-18 12:00:24.920805717 +0200
@@ -2863,8 +2863,8 @@
     . "$ac_site_file" \
       || { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "failed to load site script $ac_site_file" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "failed to load site script $ac_site_file${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
   fi
 done

@@ -4468,8 +4468,8 @@

 test -z "$CC" && { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "no acceptable C compiler found in \$PATH" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "no acceptable C compiler found in \$PATH${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }

 # Provide some information about the compiler.
 $as_echo "$as_me:${as_lineno-$LINENO}: checking for C compiler version" >&5
@@ -4583,8 +4583,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error 77 "C compiler cannot create executables" "$LINENO" 5
-as_fn_error 77 "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error 77 "C compiler cannot create executables${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 else
   { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
 $as_echo "${as__grn}yes${as__std}" >&6; }
@@ -4633,8 +4633,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of executables: cannot compile and link" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of executables: cannot compile and link${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest conftest$ac_cv_exeext
 { $as_echo "$as_me:${as_lineno-$LINENO}: result: $ac_cv_exeext" >&5
@@ -4699,8 +4699,8 @@
 	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
 as_fn_error $? "cannot run C compiled programs.
-If you meant to cross compile, use \`--host'." "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+If you meant to cross compile, use \`--host'.${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
     fi
   fi
 fi
@@ -4758,8 +4758,8 @@

 { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "cannot compute suffix of object files: cannot compile" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "cannot compute suffix of object files: cannot compile${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi
 rm -f conftest.$ac_cv_objext conftest.$ac_ext
 fi
@@ -10212,8 +10212,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C preprocessor \"$CPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C preprocessor \"$CPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -11205,9 +11205,8 @@
 	lt_prog_compiler_pic='-fPIC -DPIC'
 	lt_prog_compiler_static='-static'
 	;;
-      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran*)
-        # Portland Group compilers (*not* the Pentium gcc compiler,
-	# which looks to be a dead project)
+      pgcc* | pgf77* | pgf90* | pgf95* | pgfortran* | nvc* | nvfortran*)
+        # NVIDIA HPC Compilers
 	lt_prog_compiler_wl='-Wl,'
 	lt_prog_compiler_pic='-fpic'
 	lt_prog_compiler_static='-Bstatic'
@@ -11252,7 +11251,7 @@
 	  lt_prog_compiler_pic='-hpic'
 	  lt_prog_compiler_static='-static'
 	  ;;
-	*pgcc* | *pgf77* | *pgf90* | *pgf95* | *pgfortran*)
+	*Portland\ Group* | *NVIDIA\ Compilers* | *PGI\ Compilers*)
 	  lt_prog_compiler_wl='-Wl,'
 	  lt_prog_compiler_pic='-fpic'
 	  lt_prog_compiler_static='-Bstatic'
@@ -11901,16 +11900,16 @@
 	tmp_addflag=' $pic_flag'
 	tmp_sharedflag='-shared'
 	case $cc_basename,$host_cpu in
-        pgcc*)				# Portland Group C compiler
+        pgcc* | nvc*)				# NVIDIA HPC C compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgCC*)				# Portland Group C++ compiler
+	pgCC* | pgcpp* | pgc++* | nvc++*)				# NVIDIA HPC C++ compiler
 	  whole_archive_flag_spec='${wl}--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` ${wl}--no-whole-archive'
 	  tmp_addflag=' $pic_flag'
 	  ;;
-	pgf77* | pgf90* | pgf95* | pgfortran*)
-					# Portland Group f77 and f90 compilers
+	pgf77* | pgf90* | pgf95* | pgfortran* | nvfortran*)
+					# NVIDIA HPC Fortran compiler
 	  whole_archive_flag_spec='$wl--whole-archive`for conv in $convenience\"\"; do test  -n \"$conv\" && new_convenience=\"$new_convenience,$conv\"; done; func_echo_all \"$new_convenience\"` $wl--no-whole-archive'
 	  tmp_addflag=' $pic_flag -Mnomain' ;;
 	ecc*,ia64* | icc*,ia64*)	# Intel C compiler on ia64
@@ -15057,8 +15056,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "C++ preprocessor \"$CXXCPP\" fails sanity check${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 ac_ext=c
@@ -16027,7 +16026,7 @@
 	    whole_archive_flag_spec_CXX='$wl--whole-archive$convenience $wl--no-whole-archive'
 	    ;;
           pgCC* | pgcpp*)
-            # Portland Group C++ compiler
+            # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	    case `$CC -V` in
 	    *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	      prelink_cmds_CXX='tpldir=Template.dir~
@@ -16127,7 +16126,7 @@
 	      output_verbose_link_cmd='func_echo_all'
 	      ;;
 	    *pgCC* | *pgcpp*)
-	      # Portland Group C++ compiler
+	      # NVIDIA HPC C++ compiler. No special treatment for pgc++ and nvc++.
 	      case `$CC -V` in
 	      *pgCC\ [1-5].* | *pgcpp\ [1-5].*)
 	        prelink_cmds_CXX='tpldir=Template.dir~
@@ -16880,8 +16879,8 @@
 	    lt_prog_compiler_pic_CXX='-fPIC'
 	    lt_prog_compiler_static_CXX='-static'
 	    ;;
-	  pgCC* | pgcpp*)
-	    # Portland Group C++ compiler
+	  pgCC* | pgcpp* | pgc++* | nvc++*)
+	    # NVIDIA HPC C++ compiler
 	    lt_prog_compiler_wl_CXX='-Wl,'
 	    lt_prog_compiler_pic_CXX='-fpic'
 	    lt_prog_compiler_static_CXX='-Bstatic'
@@ -16913,8 +16912,8 @@
 	      lt_prog_compiler_static_CXX='-static'
 	      lt_prog_compiler_wl_CXX='-Wl,'
 	      ;;
-	    *pgCC* | *pgcpp*)
-	      # Portland Group/NVIDIA C++ compiler
+	    *pgCC* | *pgcpp* | *pgc++* | *nvc++*)
+	      # NVIDIA HPC C++ compiler
 	      lt_prog_compiler_pic_CXX='-fpic'
 	      lt_prog_compiler_static_CXX='-Bstatic'
 	      lt_prog_compiler_wl_CXX='-Wl,'
@@ -19058,8 +19057,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%100)/10" "_ax_c_compiler_version_minor"        ""; then :
@@ -19067,8 +19066,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__INTEL_COMPILER%10)" "_ax_c_compiler_version_patch"        ""; then :
@@ -19076,8 +19075,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_INTEL unknown intel compiler version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19124,8 +19123,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC__%100" "_ax_c_compiler_version_minor"        ""; then :
@@ -19133,8 +19132,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__/0x100" "_ax_c_compiler_version_patch"        ""; then :
@@ -19142,8 +19141,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__xlC_ver__%0x100" "_ax_c_compiler_version_build"        ""; then :
@@ -19151,8 +19150,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch.$_ax_c_compiler_version_build"
@@ -19164,8 +19163,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_release__" "_ax_c_compiler_version_minor"        ""; then :
@@ -19173,8 +19172,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         if ac_fn_c_compute_int "$LINENO" "__ibmxl_version__" "_ax_c_compiler_version_major"        ""; then :
@@ -19182,8 +19181,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

         ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19198,8 +19197,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/10000)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -19207,8 +19206,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(__xlC__/100000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -19216,8 +19215,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_IBM unknown IBM compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19232,8 +19231,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -19241,8 +19240,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PATHCC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -19250,8 +19249,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PATHSCALE unknown pathscale patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19263,8 +19262,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_minor__" "_ax_c_compiler_version_minor"        ""; then :
@@ -19272,8 +19271,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CLANG unknown clang minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__clang_patchlevel__" "_ax_c_compiler_version_patch"        ""; then :
@@ -19291,8 +19290,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc release${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "_RELEASE_MINOR" "_ax_c_compiler_version_minor"        ""; then :
@@ -19300,8 +19299,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_CRAY unknown crayc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -19324,8 +19323,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -19333,8 +19332,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__GNUC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -19342,8 +19341,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_GNU unknown gcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19362,8 +19361,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_until59" = X1; then :
@@ -19378,8 +19377,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -19393,8 +19392,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -19408,8 +19407,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -19425,8 +19424,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -19440,8 +19439,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "(
@@ -19455,8 +19454,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SUN unknown sun major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -19477,8 +19476,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if test "X$_ax_c_compiler_version_untilA0121" = X1; then :
@@ -19496,8 +19495,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -19511,8 +19510,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       if ac_fn_c_compute_int "$LINENO" "((
@@ -19526,8 +19525,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_HP unknown hp major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

       ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19547,8 +19546,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec release version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -19562,8 +19561,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "((
@@ -19577,8 +19576,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_DEC unknown dec major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19615,8 +19614,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

      if test $_ax_c_compiler_version_turboc_raw -lt 661 || test $_ax_c_compiler_version_turboc_raw -gt 1023; then :
@@ -19631,8 +19630,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	if ac_fn_c_compute_int "$LINENO" "(
@@ -19646,8 +19645,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown turboc major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

 	ax_cv_c_compiler_version="0turboc:$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -19683,8 +19682,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_BORLAND unknown borlandc version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     case $_ax_c_compiler_version_borlandc_raw in #(
@@ -19744,8 +19743,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__COMO_VERSION__/100)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -19753,8 +19752,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_COMEAU unknown comeau compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -19766,8 +19765,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/100)%10" "_ax_c_compiler_version_minor"        ""; then :
@@ -19775,8 +19774,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__KCC_VERSION/1000)%10" "_ax_c_compiler_version_major"        ""; then :
@@ -19784,8 +19783,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_KAI unknown kay compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19804,8 +19803,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -19819,8 +19818,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(
@@ -19834,8 +19833,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SGI unknown SGI compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19847,8 +19846,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(_MSC_VER/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -19856,8 +19855,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

     _ax_c_compiler_version_patch=0
@@ -19878,8 +19877,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -19891,8 +19890,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -19903,8 +19902,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_MICROSOFT unknown microsoft compiler build version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi


@@ -19917,8 +19916,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler patch version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x100)%0x10" "_ax_c_compiler_version_minor"        ""; then :
@@ -19926,8 +19925,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__MWERKS__/0x1000)%0x10" "_ax_c_compiler_version_major"        ""; then :
@@ -19935,8 +19934,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_METROWERKS unknown metrowerks compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -19947,8 +19946,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler minor version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "(__WATCOMC__/100)%100" "_ax_c_compiler_version_major"        ""; then :
@@ -19956,8 +19955,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_WATCOM unknown watcom compiler major version${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor"
@@ -19969,8 +19968,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_MINOR__" "_ax_c_compiler_version_minor"        ""; then :
@@ -19978,8 +19977,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "__PGIC_PATCHLEVEL__" "_ax_c_compiler_version_patch"        ""; then :
@@ -19987,8 +19986,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_PORTLAND unknown pgi patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
@@ -20010,8 +20009,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc major${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "/* avoid parse error with comments */
@@ -20025,8 +20024,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc minor${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   if ac_fn_c_compute_int "$LINENO" "
@@ -20043,8 +20042,8 @@
 else
   { { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
 $as_echo "$as_me: ${as__red}error: in \`$ac_pwd':${as__std}" >&2;}
-as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level" "$LINENO" 5
-as_fn_error $? "See \`config.log' for more details" "$LINENO" 5; }
+as_fn_error $? "_AX_COMPILER_VERSION_SDCC unknown sdcc patch level${as__std}
+${as__red}See \`config.log' for more details" "$LINENO" 5; }
 fi

   ax_cv_c_compiler_version="$_ax_c_compiler_version_major.$_ax_c_compiler_version_minor.$_ax_c_compiler_version_patch"
